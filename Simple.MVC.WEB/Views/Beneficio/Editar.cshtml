@model Simple.MVC.Business.Seguranca.Beneficio
@using Simple.MVC.Business.Util;
@{ 
    ViewBag.Title = "Editar - Benefício";
}
<div class="page-header">
    <h1>@ViewBag.Title</h1>
</div>
@using (Html.BeginForm())
{
@Html.HiddenFor(m => m.Id)
    @Html.ValidationSummary("Validação", new { @class="alert alert-danger summary" })

    <div class="row">
        <div class="col-xs-12">
            <div class="form-group">
                @Html.LabelFor(m => m.IdAgenteParceiro)*
                @Html.DropDownListFor(model => model.IdAgenteParceiro, new SelectList(ViewBag.IdAgenteParceiro, "Id", "CNPJ", Model.IdAgenteParceiro), "Selecione... ", new { @class = "form-control"})
            </div>
        </div>
        <div class="col-xs-12">
            <div class="form-group">
                @Html.LabelFor(m => m.Descricao)*
                @Html.EditorFor(model => model.Descricao, new { htmlAttributes = new { @class = "form-control"} })
            </div>
        </div>
        <div class="col-xs-12">
            <div class="form-group">
                @Html.LabelFor(m => m.Valor)*
                @Html.EditorFor(model => model.Valor, new { htmlAttributes = new { @class = "form-control"} })
            </div>
        </div>
        <div class="col-xs-12">
            <div class="form-group">
                @Html.LabelFor(m => m.EhPorcentagem)*
                @Html.EditorFor(model => model.EhPorcentagem, new { htmlAttributes = new { @class = "form-control"} })
            </div>
        </div>
    </div>
    <div class="form-group well">
        <button type="submit" class="btn btn-primary">Salvar</button>
        <a href="@Url.Action("Indice")" class="btn btn-default">Voltar</a>
    </div>
}

@section scripts{}

